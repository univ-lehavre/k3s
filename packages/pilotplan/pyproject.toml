[project]
name = "pilotplan"
version = "0.3.1"
description = "Declarative planning engine for pilot"
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2.11.0",
    "ruamel.yaml>=0.18.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

